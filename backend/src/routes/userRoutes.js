const r=require('express').Router();const {body}=require('express-validator');const c=require('../controllers/userController');const {protect}=require('../middleware/authMiddleware');const {validate}=require('../middleware/validationMiddleware');const {asyncHandler:a}=require('../middleware/errorMiddleware');r.use(protect);r.get('/profile',a(c.getProfile));r.put('/profile',[body('name').optional().trim().isLength({min:2,max:100}),body('phone').optional().matches(/^\+?[1-9]\d{7,14}$/),validate],a(c.updateProfile));module.exports=r;
