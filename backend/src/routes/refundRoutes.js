const r=require('express').Router();const {body}=require('express-validator');const c=require('../controllers/refundController');const {protect}=require('../middleware/authMiddleware');const {validate}=require('../middleware/validationMiddleware');const {asyncHandler:a}=require('../middleware/errorMiddleware');r.use(protect);r.get('/',a(c.mine));r.post('/',[body('booking_id').isInt({min:1}),body('reason').trim().isLength({min:5,max:500}),validate],a(c.request));module.exports=r;
