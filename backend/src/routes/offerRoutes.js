const r=require('express').Router();const {body}=require('express-validator');const c=require('../controllers/offerController');const {validate}=require('../middleware/validationMiddleware');const {asyncHandler:a}=require('../middleware/errorMiddleware');r.get('/',a(c.list));r.post('/validate',[body('code').trim().notEmpty(),body('amount').isFloat({min:0}),validate],a(c.validate));module.exports=r;
