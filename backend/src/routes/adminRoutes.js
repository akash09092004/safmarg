const r=require('express').Router();const {body}=require('express-validator');const c=require('../controllers/adminController');const {protect}=require('../middleware/authMiddleware');const {adminOnly}=require('../middleware/adminMiddleware');const {validate}=require('../middleware/validationMiddleware');const {asyncHandler:a}=require('../middleware/errorMiddleware');r.use(protect,adminOnly);r.get('/dashboard',a(c.dashboard));r.get('/users',a(c.users));r.patch('/users/:id/status',[body('is_active').isBoolean(),validate],a(c.setUserActive));r.get('/bookings',a(c.bookings));r.post('/flights',a(c.createFlight));r.put('/flights/:id',a(c.updateFlight));r.delete('/flights/:id',a(c.deleteFlight));r.get('/refunds',a(c.refunds));r.patch('/refunds/:id',[body('status').isIn(['approved','rejected','processed']),validate],a(c.updateRefund));r.post('/offers',a(c.createOffer));r.delete('/offers/:id',a(c.deleteOffer));module.exports=r;
